<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 两个以上会报错。 协议，注册中心可以多个，以及服务提供者可以多个 -->
    <dubbo:application name="xml-demo-consumer-injvm"/>


    <!-- 注册中心 -->
    <dubbo:registry address="N/A"/>

    <!-- 同一个JVM进程暴露服务 -->
    <bean id="xmlServiceImpl" class="com.code.resource.reading.consumer.xml.local.DemoServiceImpl"/>
    <dubbo:service interface="com.code.resource.reading.api.DemoService" ref="xmlServiceImpl"
                   scope="local"/>

    <!-- 引入本地服务 -->
    <dubbo:reference interface="com.code.resource.reading.api.DemoService" id="demoService"
                     scope="local"/>


</beans>