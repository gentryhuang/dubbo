<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 两个以上会报错。 协议，注册中心可以多个，以及服务提供者可以多个 -->
    <dubbo:application name="xml-demo-consumer"/>

    <!-- 注册中心 -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181"/>

    <!-- <dubbo:registry protocol="redis" address="127.0.0.1:6379"/> -->

    <!-- 引入服务-->

    <dubbo:reference interface="com.code.reading.service.IDemoService" id="proxyDemoService" check="false"/>


    <!--
     <dubbo:reference interface="com.code.resource.reading.api.DemoService" id="xmlConsumerService"  check="false"/>
     <dubbo:reference interface="com.code.resource.reading.api.DemoService" id="consumerService" check="false" injvm="true"/>
      -->

</beans>