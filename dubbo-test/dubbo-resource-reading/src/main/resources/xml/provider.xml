<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 两个以上会报错。 协议，注册中心可以多个，以及服务提供者可以多个 -->
    <dubbo:application name="xml-demo-provider"/>

    <!-- 注册中心 -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181"/>

   <!-- <dubbo:registry protocol="redis" address="127.0.0.1:6379"/> -->


    <!-- 协议 -->
    <dubbo:protocol name="dubbo" port="20880"/>

    <!-- 模块 -->
   <!-- <dubbo:module name="my_module"/> -->


    <!-- 服务提供者-->
    <bean id="xmlService" class="com.code.resource.reading.provider.xml.xmlServiceImpl"/>
    <dubbo:service interface="com.code.resource.reading.api.DemoService" ref="xmlService" scope="local"/>


</beans>